name: Keep-Alive Bot

on:
  schedule:
    # Roda a cada 5 minutos (o mínimo permitido pelo GitHub)
    - cron: '*/5 * * * *'

jobs:
  ping-my-site:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the website URL to keep it alive
        run: curl -sS https://agendamento-online-barbearia-lucas-borges.onrender.com/ > /dev/null
